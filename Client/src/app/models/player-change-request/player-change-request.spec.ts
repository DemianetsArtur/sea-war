import { PlayerChangeRequest } from './player-change-request';

describe('SquareChangeRequest', () => {
  it('should create an instance', () => {
    expect(new PlayerChangeRequest()).toBeTruthy();
  });
});
