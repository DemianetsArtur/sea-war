import { NotificationMessages } from './notification-messages';

describe('NotificationMessages', () => {
  it('should create an instance', () => {
    expect(new NotificationMessages()).toBeTruthy();
  });
});
